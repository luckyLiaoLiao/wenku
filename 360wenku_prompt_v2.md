# 提示词：360文库 AI 文档平台高保真前端原型（V2）

请用 React（JSX）生成一个完整的高保真前端原型，模拟"360文库"AI文档平台的核心交互流程。使用模拟数据，不调用真实API。整体风格参考现代SaaS文档工具（如Notion、飞书文档），视觉上要精致、专业，不要有廉价感。

---

## 一、全局结构

### 顶部导航栏
- 左侧：360文库 Logo（文字即可）
- 中间：两个主Tab切换按钮——「搜索文档」和「AI 生成」，当前选中Tab有明显高亮和下划线指示
- 右侧：用户头像 + "会员中心"按钮（纯UI，不需要功能）

### 整体布局
- 顶部导航栏固定
- 下方内容区域根据当前Tab切换显示不同内容
- 配色方案：主色用 #1A73E8（蓝色），辅助色用 #F5F7FA（浅灰背景），强调色用 #FF6D00（橙色，用于CTA按钮和付费提示）

---

## 二、搜索文档 Tab

### 2.1 默认状态（未搜索）

**搜索栏区域**（页面上部居中，占页面宽度60%）：
- 大搜索框，placeholder："搜索报告、合同、方案、论文..."
- 搜索框右侧有搜索按钮
- 搜索框下方展示一行"热门搜索"标签，如：可行性研究报告、劳动合同模板、商业计划书、年终工作总结、毕业论文模板（可点击，点击后自动填入搜索框并触发搜索）

**分类推荐区域**（搜索栏下方）：
- 标题："热门文档分类"
- 用卡片网格布局（每行4个），每张卡片包含：
  - 图标（用emoji代替即可）
  - 分类名称
  - 文档数量（如"12,580篇"）
- 分类数据（模拟）：
  - 📊 可行性报告 | 12,580篇
  - 📋 合同协议 | 28,340篇
  - 💼 商业计划书 | 8,920篇
  - 📝 工作总结 | 45,120篇
  - 🎓 学术论文 | 32,670篇
  - 📑 项目方案 | 15,890篇
  - 🏗️ 工程文档 | 9,450篇
  - 📄 政府公文 | 11,230篇

### 2.2 搜索结果状态

用户输入关键词并搜索后，页面变为：

**搜索栏**保持在顶部（缩小为窄条形式）

**筛选栏**（搜索栏下方，横向排列）：
- 文档类型：全部 / 报告 / 合同 / 方案 / 论文 / 总结
- 格式：全部 / Word / PPT / PDF / Excel
- 排序：综合排序 / 下载量最多 / 最新发布
- 每个筛选项用下拉或标签切换形式

**搜索结果列表**：
- 每条结果是一个横向卡片，包含：
  - 左侧：文档类型图标 + 格式标签（如 Word、PDF）
  - 中间：文档标题（关键词高亮）、摘要（2行，超出截断）、上传者昵称 + 上传时间
  - 右侧：下载量、"预览"按钮（次要样式）、"下载"按钮（主要样式）
- 展示5-6条模拟搜索结果

**搜索无结果状态**（当搜索词为"量子计算可行性"时触发）：
- 显示空状态插图（用文字代替即可）
- 文案："未找到相关文档"
- 下方有醒目的引导按钮："让 AI 帮你生成一份 →"，点击后自动切换到「AI 生成」Tab

### 模拟数据

搜索"可行性"时返回以下结果：
1. 工程项目可行性研究报告（Word）- 摘要：本报告对XX工程项目进行了全面的可行性分析... - 下载量 3,280
2. 政府投资项目可行性研究报告模板（PDF）- 下载量 5,120
3. 互联网产品可行性分析报告（Word）- 下载量 1,890
4. 新能源汽车充电桩项目可行性报告（Word）- 下载量 2,450
5. 农业产业化项目可行性研究（PDF）- 下载量 1,230

其他任何搜索词都返回类似的通用模拟结果即可。

---

## 三、AI 生成 Tab

这是产品核心。核心设计理念：**用户只需选择行业，即可立即获得一份带大纲、带内容的完整文档，可直接使用也可编辑后重新生成。** 最大限度降低用户的前置操作成本，把选择权交给用户——赶时间直接用，追求质量就编辑细化。

整体流程：文档类型选择 → 行业选择 → 立即生成完整文档 → 编辑与重新生成。

### 3.1 第一步：文档类型选择

**页面标题**："选择你要生成的文档类型"

**卡片网格**（每行3个，带hover效果）：
每张卡片包含：
- 大图标（emoji）
- 文档类型名称
- 一句话描述
- "已生成 XX 份"（社会证明）

卡片数据：
1. 📊 可行性研究报告 | "全面分析项目可行性，支持立项决策" | 已生成 28,450 份
2. 💼 商业计划书 | "面向投资人的专业商业计划" | 已生成 15,230 份
3. 📋 合同协议 | "各类标准合同与协议模板" | 已生成 42,100 份
4. 📝 工作总结 | "年度/季度/月度工作总结" | 已生成 56,780 份
5. 📑 项目方案 | "项目规划与实施方案" | 已生成 19,340 份
6. 🎓 学术论文 | "论文框架与内容生成" | 已生成 12,560 份

点击任意卡片进入第二步。以下以"可行性研究报告"为例。

### 3.2 第二步：行业选择（唯一必填项）

**页面标题**："选择行业领域"
**副标题**："选择后将立即为您生成一份完整的可行性研究报告"

**行业选择卡片**（每行4个，单选，点击即选中高亮）：
- 🏗️ 工程建设
- 🏛️ 政府投资
- 💻 互联网/IT
- 🏭 制造业
- 🌱 农业
- ⚡ 新能源
- 🏥 医疗健康
- 📦 其他（点击后出现输入框让用户自定义）

**底部按钮**：
- "立即生成"（橙色CTA按钮，选择行业后激活）
- "返回"（文字按钮，回到文档类型选择）

**点击"立即生成"后**：显示生成中过渡动画（2-3秒），进度条 + 文案"AI 正在为您生成报告..."，然后直接进入完整的文档编辑页面。

### 3.3 第三步：文档编辑页面（核心体验）

这是核心页面，左右分栏布局。用户进入时看到的是一份已经生成好的、带完整大纲和内容的文档。用户可以直接使用，也可以编辑任意章节后重新生成。

**顶部工具栏**：
- 左侧：
  - "← 返回"按钮（回到文档类型选择）
  - 文档标题（可编辑的输入框）——默认显示如"工程建设项目可行性研究报告"
- 右侧按钮组：
  - "⚙️ 高级设置"（次要按钮，点击弹出侧边抽屉，详见3.4）
  - "重新生成全文"（次要按钮）
  - "导出 Word"（橙色主要按钮）
  - "保存草稿"（文字按钮）

**左侧面板（宽度约25%）**：文档大纲导航
- 面板标题："文档大纲"
- 树状结构，显示报告的各章节标题
- 每个章节标题前有数字编号
- 点击某章节，右侧平滑滚动到对应位置
- 当前查看的章节高亮显示（蓝色左边框）
- 章节列表（基于用户选择的"工程建设"行业自动生成）：
  1. 总论
  2. 项目背景与必要性
  3. 市场分析与需求预测
  4. 项目选址与建设条件
  5. 技术方案
  6. 环保与安全
  7. 项目组织与管理
  8. 投资估算与资金筹措
  9. 财务分析
  10. 风险评估与对策
  11. 结论与建议

**右侧面板（宽度约75%）**：分段内容编辑区
- 每个章节是一个独立的内容块（白色卡片样式），包含：
  - 章节标题（如"一、总论"），加粗显示
  - AI生成的内容（3-5段模拟文本，内容要看起来像真实的可行性报告，不要用lorem ipsum。根据用户选择的行业"工程建设"生成对应行业风格的内容）
  - 每个内容块右上角有操作按钮（hover时显示）：
    - 🔄 "重新生成本段"按钮
    - ✏️ "编辑"按钮
- 内容块之间有间距分隔

**编辑交互**：
- 点击"编辑"按钮后：
  - 该章节内容区变为可编辑的textarea
  - 出现"保存"和"取消"按钮
  - 保存后内容更新，恢复为展示状态
  - 已被用户编辑过的章节，左上角显示一个小标记"✎ 已编辑"（绿色），表示这是用户确认过的内容

**重新生成交互**：
- 点击某章节的"重新生成本段"：该章节显示骨架屏loading 1-2秒，然后展示新内容（和原内容略有不同）
- 点击顶部"重新生成全文"：弹出确认对话框"重新生成将覆盖当前内容（已编辑的章节将保留），确认重新生成？"，确认后全部未编辑章节重新loading并更新内容，已编辑章节保持不变

**右侧面板底部**：
- 固定提示条："💡 直接编辑任意章节内容，修改后点击「重新生成全文」，AI 将参考您的修改生成更精准的内容"

### 3.4 高级设置（可选，侧边抽屉）

点击顶部工具栏的"⚙️ 高级设置"按钮，从右侧滑出一个抽屉面板，包含以下可选配置项。这些配置不是必填的，用户可以完全不碰，也可以按需调整。

**抽屉标题**："高级设置"
**副标题**："调整以下选项可以让生成结果更贴合您的需求"

**设置项**：

1. **报告用途**（单选，默认"不限"）：
   - 不限
   - 立项审批
   - 融资申请
   - 内部决策参考
   - 政府备案

2. **投资规模**（单选，默认"不限"）：
   - 不限
   - 100万以下
   - 100-500万
   - 500-1000万
   - 1000万-1亿
   - 1亿以上

3. **项目周期**（单选，默认"不限"）：
   - 不限
   - 6个月以内
   - 6-12个月
   - 1-3年
   - 3年以上

4. **报告详细程度**（单选，默认"标准版"）：
   - 精简版（约8-10页）
   - 标准版（约15-20页）
   - 详细版（约30-40页）

5. **补充说明**（多行文本框，placeholder："可填写项目名称、项目背景、特殊要求等任何补充信息..."）

**抽屉底部按钮**：
- "应用并重新生成"（橙色主要按钮，点击后关闭抽屉，全文重新生成）
- "取消"（文字按钮，关闭抽屉不做任何操作）

---

## 四、交互细节要求

1. **Tab切换**：搜索和生成Tab切换时有平滑过渡
2. **搜索无结果导流**：搜索"量子计算可行性"时显示空状态，引导按钮点击后跳转到AI生成Tab
3. **大纲点击定位**：左侧大纲点击章节时，右侧平滑滚动到对应章节
4. **大纲滚动联动**：右侧内容区滚动时，左侧大纲自动高亮当前可见的章节
5. **编辑状态切换**：点击编辑按钮后内容变为textarea，保存后恢复展示状态并显示"已编辑"标记
6. **重新生成动画**：点击"重新生成本段"时，内容区显示骨架屏loading效果1-2秒后展示新内容
7. **重新生成全文保护**：点击"重新生成全文"时，已编辑的章节不被覆盖，仅未编辑章节重新生成
8. **高级设置抽屉**：从右侧平滑滑入，有半透明遮罩层，点击遮罩可关闭
9. **热门搜索标签点击**：点击后自动填入搜索框并显示搜索结果
10. **分类卡片点击**：点击搜索Tab中的分类卡片，自动以该分类名称搜索
11. **行业选择即时反馈**：选中行业卡片时有选中态动画（边框高亮+勾选图标），未选择行业时"立即生成"按钮为禁用态

## 五、视觉风格要求

- 整体白色底 + 浅灰区块
- 圆角卡片（border-radius: 12px）
- 适当的阴影（box-shadow）增加层次感
- 字体层级清晰：标题用大号加粗，正文用14-15px，辅助信息用灰色小字
- 按钮分三级：主要（蓝色填充）、次要（蓝色描边）、文字按钮
- CTA按钮（如"立即生成""导出Word"）用橙色 #FF6D00
- 动画和过渡要自然流畅，不要生硬
- 文档编辑页面的左侧大纲面板用浅灰背景区分
- 已编辑章节的"✎ 已编辑"标记用绿色（#4CAF50）

## 六、技术要求

- 使用 React + Tailwind CSS
- 所有数据使用硬编码的模拟数据，不调用任何外部API
- 组件结构清晰，状态管理用 React useState/useReducer
- 确保所有交互流程都能跑通
- 页面宽度适配桌面端（1200px-1440px）
